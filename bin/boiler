#!/bin/bash

export_boiler_path() {
    if [[ -z "$BOILER_PATH" ]]; then
        export BOILER_PATH=$HOME/boiler
    fi
}

export PROJECT_PATH=$PWD \
    && export_boiler_path \
    && npm --prefix /usr/local/src/boiler run --silent start -- $*